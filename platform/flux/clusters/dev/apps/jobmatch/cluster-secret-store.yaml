apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: gcp-secret-manager
spec:
  provider:
    gcpsm:
      projectID: "des-job-searcher"
      auth:
        workloadIdentity:
          serviceAccountRef:
            name: "external-secrets-external-secrets"
            namespace: "external-secrets"